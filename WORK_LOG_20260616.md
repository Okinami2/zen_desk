# Zen Desk - 毫米波雷达与多模态控制开发日志

## 1. 阶段达成效果概览
自开展毫米波雷达相关的开发联调工作以来，我们已经成功打通了从底层雷达硬件串口数据采集、解析、状态机判定，再到顶层多模态融合控制（语音 + 雷达联动）的全链路。

目前的 Zen Desk 已经具备了以下效果：
*   **全天候精准感知**：通过毫米波雷达的静止能量和运动能量分离分析，系统能精确感知用户的在座、离座、小幅运动及大幅动作。
*   **智能丝滑的灯光联动**：当感知到用户彻底离座时，台灯会在 2 秒内优雅、平滑地渐灭；当用户回来入座时，台灯会自动恢复到离座前一模一样的光照状态。
*   **无阻塞的多模态并发**：彻底解决了原先语音识别和雷达状态“争抢通信通道”导致的卡死问题，现在的管家服务可以同时、实时响应语音命令和雷达心跳。

---

## 2. 完成的开发进度与技术攻坚点

### 2.1 底层解析与防粘包重构 (`radar_service`)
*   **141字节扩展协议适配**：发现了雷达运行在扩展工程模式下，重写了 `radar_protocol.c`，精准匹配包头 `F4F3F2F1` 和包尾 `F8F7F6F5`，并精确校准了目标有无、距离、16个距离门的运动能量和静止能量的偏移量读取。
*   **串口数据防粘包缓冲池**：重构了串口读取逻辑，将重叠缓冲区大小扩大至 `140` 字节。即使雷达数据在内核态被截断，程序也能将其与下一次读取的数据完美拼接，彻底解决了由于雷达高速吐数据导致的丢帧、解析卡死问题。

### 2.2 TCP 服务端并发改造 (`fusion_service`)
*   **多线程模型升级**：原有的 `fusion_service` 为单线程阻塞模型，雷达接入后会堵死语音（ASR）的指令下发。我们引入了 `pthread`，每当有新模块（雷达、语音或其它传感器）接入时，都会开启一个独立线程为其服务，实现了多端并发。

### 2.3 融合控制与平滑执行逻辑 (`device_service`)
*   **快照恢复机制**：新增了 `g_saved_radar_scene` 结构。在触发离座关灯（`STATE_ABSENT`）的前一瞬间，拍下当前的亮度、色温和呼吸模式快照；待用户返回（`STATE_SEATED_IDLE`）时，完美读取快照并恢复。
*   **平滑渐变算法**：修改了台灯控制接口，支持在给定的毫秒数内（如 2000ms）通过 PWM 细滑地改变亮度，告别了突兀的闪烁式开关。

---

## 3. 语音与雷达的控制逻辑关系

在目前的工程实现中，我们设计了一套**“基于用户明确意图的优先级接管机制”**。

### 3.1 语音优先逻辑 (`g_voice_override`)
*   **最高优先级锁定**：当用户通过语音下发了明确的灯光指令（如“开灯”、“关灯”、“开启呼吸灯”）时，系统会认为这是用户最高级别的显性意图，立即触发 `g_voice_override = 1`，并记录此时的语音场景。
*   **效果**：只要用户一直坐在座位上，雷达检测到的普通微小状态变化将**不再**随意改变灯光（比如不会擅自切换到阅读灯），而是坚定地维持语音定下的状态。如果用户语音要求“关灯”，那么只要人不离开，灯就会一直保持关闭。

### 3.2 雷达伴随逻辑
*   **离座关灯**：一旦雷达状态机判定连续一段时间无人（`AWAY`），状态变为 `STATE_ABSENT`，系统无论当前灯光是什么状态，都会强制在 2 秒内渐灭至完全关闭，以达到节能的目的。
*   **归来恢复**：一旦用户回来入座，系统会先检查离座前的“状态快照”。此时，由于之前的最高优先级“语音状态”也被保存在了快照中，所以回来时依然能丝毫不差地还原离开前无论是智能联动还是语音设定的最终状态。

---

**下一步待优化方向记录**：
1. 是否需要在“用户彻底离座再回来”时，解除之前的语音锁定（如语音关灯后离开再回来，强制点亮一盏阅读灯）。
2. 雷达能量状态机（FSM）的阈值和时间窗口大小，在大量实机测试后可进一步外挂至配置文件中以便灵活调参。
